{
  "name": "backend",
  "version": "1.0.0",
  "description": "backend for carsApp",
  "main": "server.js",
  "scripts": {
    "server": "node --env-file=.env server.js --env-file",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "sqlite3": "^5.1.7"
  }
}
